name: geodiary
description: A location-based journaling app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Update based on your Flutter version

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2 # For date formatting, use latest
  google_fonts: ^6.2.0 # Optional, like MealApp, use latest

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/ # Add if you have local assets